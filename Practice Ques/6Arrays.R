# OBJECTS IN R
# Objects can be Vectors, Lists, Matrices, Arrays, Factors or Data Frames

# Arrays - data object that can store data in more than two dimensions

threeDMatrix <- array(data = 1:24, dim=c(3,4,2)) # dim = c(row,col,num of arrays)
threeDMatrix