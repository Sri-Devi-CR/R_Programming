# MOD 2

#piechart
#col <- table(dataset$target_col)
#labs <- paste(names(col), '\n', col, sep='')
#pie(col,
#  labels = lab,
#  main = 'title')

head(mtcars)
data <- read.csv(file.choose(), header = TRUE)
